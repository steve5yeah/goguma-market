# Vercel에도 이 이름 그대로 넣습니다. (NEXT_PUBLIC_ 접두사 없음)
# 브라우저로 값을 넘기는 일은 next.config.ts가 대신 해 줍니다.
SUPABASE_URL=여기에_SUPABASE_URL
SUPABASE_ANON_KEY=여기에_PUBLISHABLE_KEY

# 배포 주소 (메일 인증 링크가 돌아올 주소). 비워 두면 접속한 주소를 그대로 씁니다.
SITE_URL=http://localhost:3000
