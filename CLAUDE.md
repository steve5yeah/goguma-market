# 고구마마켓 🍠 — 작업 규칙

개발을 배우는 중인 **비개발자**와 함께 만드는 연습용 프로젝트입니다.
Claude는 아래 규칙을 항상 지켜 주세요.

---

## 1. 말투 · 설명 방식

- **모든 대화는 한국어로.**
- 개발 용어가 나오면 **꼭 쉬운 말로 풀어서** 설명합니다.
  - 예) "마이그레이션" → "데이터베이스 구조를 바꾸는 작업(표에 칸 추가하기 같은 것)"
  - 예) "환경 변수" → "비밀번호처럼 코드에 직접 적으면 안 되는 값을 따로 적어 두는 메모장(`.env.local` 파일)"
- 파일 경로나 명령어는 그대로 보여 주되, **이게 뭘 하는 건지 한 줄 설명**을 붙입니다.
- 어려운 영어 약어(RLS, SSR, CI 등)는 처음 쓸 때 괄호로 뜻을 적어 줍니다.

## 2. 진행 상황 자주 알리기

- 작업 시작 전: **"지금부터 ○○를 만들겠습니다"** 라고 먼저 말합니다.
- 작업 중간중간: **"지금 ○○ 파일을 고치는 중입니다"** 처럼 짧게 현재 위치를 알립니다.
- 작업 후: **무엇이 바뀌었는지, 화면에서 뭐가 달라 보이는지** 를 정리해서 알려 줍니다.
- 조용히 오래 작업하지 않습니다. 사용자가 "지금 뭐 하는 중이지?" 하고 궁금해지지 않게 합니다.

## 3. 단계적으로만 진행

- **사용자가 시킨 것만 합니다.** 시키지 않은 기능은 미리 만들지 않습니다.
- "다음 단계는 ○○인데 지금 해 드릴까요?" 하고 **물어보고 기다립니다.**
- 한 번에 여러 단계를 몰아서 하지 않습니다. 한 단계 끝 → 확인 → 다음 단계.
- 개선하고 싶은 게 보이면 **바로 고치지 말고 먼저 제안**합니다.

## 4. 할 수 있는 건 최대한 Claude가

- 파일 만들기 · 고치기, 패키지 설치, 개발 서버 실행, 데이터베이스 표 만들기(Supabase MCP), 화면 확인, git 커밋 → **전부 Claude가 직접** 합니다.
- **사람만 할 수 있는 일**(웹사이트에 로그인해서 버튼을 눌러야 하는 것 등)만 부탁합니다.
  이때는 이렇게 안내합니다:
  1. 어느 사이트에 들어가서
  2. 어느 메뉴를 누르고
  3. 무엇을 입력/복사해서
  4. 그 결과를 어떻게 알려 주면 되는지
- 사람이 해야 하는 일 예시: Supabase 대시보드 설정 변경, Vercel 계정 연결, GitHub 저장소 만들기, 결제·비밀번호 입력.

## 5. 다 만들면 바로 열어서 보여 주기

- 코드를 고친 뒤에는 **반드시 브라우저로 직접 열어서 동작을 확인**합니다.
  - `preview_start` 로 `goguma-dev` 서버를 켜고 (`.claude/launch.json`에 설정되어 있음)
  - 화면을 읽거나 스크린샷을 찍어 **"이렇게 나옵니다"** 하고 보여 줍니다.
- 사용자에게 "직접 확인해 보세요"라고 떠넘기지 않습니다. Claude가 먼저 확인하고 결과를 보여 줍니다.
- 에러가 있으면 **브라우저 콘솔 · 서버 로그까지 확인**하고 고친 뒤 다시 보여 줍니다.

---

## 프로젝트 정보

| 항목 | 내용 |
| --- | --- |
| 만드는 것 | 우리 동네 중고거래 웹사이트 (당근마켓 같은 것) |
| 화면 만드는 도구 | **Next.js 15** (App Router) + React 19 + TypeScript |
| 디자인 | **Tailwind CSS v4** — 색은 `src/app/globals.css`의 `@theme`에 정의 (`goguma-*`, `skin-*`, `soil-*`) |
| 회원·데이터 보관 | **Supabase** (로그인, 데이터베이스, 파일 저장) |
| 인터넷 공개(배포) | **Vercel** — https://goguma-market-teal.vercel.app (GitHub: steve5yeah/goguma-market) |
| 실행 | `npm run dev` → http://localhost:3000 |

### Supabase 주의사항 ⚠️

- **가계부 프로젝트와 같은 Supabase 프로젝트**(`gagyebu`, `fpffmmbuiomcfdrjxnck`)를 함께 씁니다.
- 고구마마켓이 만드는 표(테이블)에는 **반드시 `goguma_` 접두사**를 붙입니다.
- 가계부 표(`entries`, `ledger_meta`)는 **절대 건드리지 않습니다.**
- 지금 Confirm email(메일 인증)은 **꺼져 있습니다** — 가입하면 바로 로그인됩니다.

### 폴더 구조

```
src/
├─ middleware.ts          로그인 상태 유지 + /mypage, /favorites, /products/new 보호
├─ lib/supabase/          Supabase 연결 코드 (client / server / middleware)
├─ app/
│  ├─ layout.tsx          모든 화면 공통 틀 (헤더·푸터)
│  ├─ globals.css         색 팔레트
│  ├─ page.tsx            홈
│  ├─ auth/               가입·로그인·로그아웃 처리
│  ├─ chat/               채팅방 목록·대화 화면 (실시간)
│  ├─ login/ signup/      로그인·가입 화면
│  ├─ products/           거래글 목록·상세·등록·수정 (찜하기 포함)
│  ├─ favorites/          찜한 물건
│  └─ mypage/             로그인해야 보이는 내 정보
└─ components/            여러 화면에서 같이 쓰는 조각들
```

### 진행 단계

- [x] **1단계** — 회원가입 / 로그인 / 로그아웃
- [x] **2단계** — 상품 등록 · 목록 · 상세 · 수정 · 삭제 (사진은 Supabase Storage)
- [x] **3단계(1)** — 찜하기
- [x] **3단계(2)** — 채팅
- [x] **4단계** — 동네 설정 · 조회수 · 프로필 수정 · 디자인 정리
- [x] **배포** — Vercel에 올려서 인터넷에 공개

### git 커밋

- 커밋 메시지는 **한국어**로, `1단계: 회원가입 / 로그인 / 로그아웃` 처럼 단계와 내용을 적습니다.
- 커밋·푸시는 **사용자가 요청할 때만** 합니다.
